# 一周 Research Agent 项目依赖
# 核心依赖
zhipuai>=2.1.0          # 智谱 GLM 官方 SDK（含 chat + web_search）
python-dotenv>=1.0.0   # 从 .env 读取环境变量
sniffio>=1.3.0         # zhipuai 的传递依赖（httpx 需要）
ddgs>=7.0.0            # DuckDuckGo 搜索（Day 3+ 真实联网用，免费无需 key）
readability-lxml>=0.8  # 网页正文提取（Day 5+ fetch_url 工具用，自动去广告/导航）

# Day 11 RAG
chromadb>=0.5.0                 # 向量数据库（本地文件存储，零配置）
sentence-transformers>=2.7.0    # 本地 Embedding 模型（免费离线）

# Day 7 部署用
fastapi>=0.110.0        # Web 框架
uvicorn>=0.27.0         # ASGI 服务器
