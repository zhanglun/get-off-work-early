# 项目目录说明

> 用途：快速了解根目录各学习/项目文件夹的定位、当前状态和推荐入口，方便后续继续研究。
>
> 根目录总索引仍以 [`README.md`](README.md) 为准；本文件只负责解释目录，不替代各目录内部的详细进度文档。

## 一、根目录总览

| 目录 | 定位 | 当前状态 | 推荐入口 |
| --- | --- | --- | --- |
| [`week-research-agent/`](week-research-agent/) | 手写 Research Agent 学习项目 | 核心能力已完成 | [`README.md`](week-research-agent/README.md) |
| [`mastra-playground/`](mastra-playground/) | Mastra 通用 API 和 Tool Calling 实验 | Lesson 01-02 已完成 | [`Mastra-学习笔记.md`](mastra-playground/Mastra-学习笔记.md) |
| [`mastra-short-drama-agent/`](mastra-short-drama-agent/) | 当前唯一活跃的短剧 Agent 主项目 | 方案已成型，持续开发 | [`README.md`](mastra-short-drama-agent/README.md)、[`CONTEXT.md`](mastra-short-drama-agent/CONTEXT.md) |
| [`agent-base/`](agent-base/) | NestJS 分镜 Agent 历史底座和业务基线 | 已冻结，作为参考 | [`学习进度.md`](agent-base/学习进度.md) |
| [`agent-tool-calls/`](agent-tool-calls/) | Agent Tool 调用失败与恢复专题 | 研究和图示已完成 | [`agent-tools-failure-lifecycle.md`](agent-tool-calls/agent-tools-failure-lifecycle.md) |
| [`agent-activity-stream/`](agent-activity-stream/) | Agent 活动流和 Codex 风格交互专题 | 方案研究完成，落地待继续 | [`agent-activity-stream-codex-style.md`](agent-activity-stream/agent-activity-stream-codex-style.md) |
| [`coding-agent-ttft/`](coding-agent-ttft/) | Coding Agent 首响应延迟调研框架 | 研究框架已建立，内容待补 | [`outline.yaml`](coding-agent-ttft/outline.yaml)、[`fields.yaml`](coding-agent-ttft/fields.yaml) |
| [`embodied-data-loop/`](embodied-data-loop/) | 具身智能和机器人数据闭环学习线 | 当前暂停 | [`README.md`](embodied-data-loop/README.md)、[`学习进度.md`](embodied-data-loop/学习进度.md) |
| [`ai-platform-interview-handbook/`](ai-platform-interview-handbook/) | AI Platform / Tech Lead 面试推演手册 | 内容已完成 | [`README.md`](ai-platform-interview-handbook/README.md) |

## 二、各目录说明

### 1. `week-research-agent/`：手写 Research Agent

**是什么**

从零实现一个能够联网搜索、多步调用工具并生成研究报告的 Python Agent。它是 Agent 学习路线中最重要的原理型项目，不依赖 LangChain 或 LangGraph 来隐藏核心机制。

**已经覆盖**

- Agent = `LLM + Tool + Loop`；
- Tool Schema 与 Tool Registry；
- 单工具和多工具调用；
- 多轮 Agent Loop；
- State、`max_steps` 和死循环保护；
- 真实 Web Search 与网页全文读取；
- 日志、重试、结构化输出和错误处理；
- Research Agent、Evaluation、SSE 和 SQLite 持久化；
- Token 使用量和成本统计。

**目录内重点**

- `day1/`：最小单工具 Agent；
- `day2/`：多 Tool Calling 和连续调用；
- `day3/`：State、多步执行和真实搜索；
- `day4/`：健壮性、日志、重试；
- `research_agent/`：完整 Research Agent；
- `rag/`：RAG 实验；
- `workflow/`：Planner / Executor / Synthesizer；
- `evaluation/`：Agent 评估；
- `server/`：FastAPI、SSE 和持久化服务；
- `langgraph_version/`：LangGraph 最小 StateGraph 对照实验。

**后续适合研究**

优先补 `mcp/`，学习手写 MCP Server、官方 SDK、MCP Client，以及把外部 MCP 工具动态转换为现有 Agent Tool Schema。

---

### 2. `mastra-playground/`：Mastra 基础实验场

**是什么**

独立的 TypeScript 沙盒，用于学习 Mastra 的 Agent、Tool、Schema、Runtime 和 Studio API，不承载短剧业务代码。

**已经覆盖**

- `createTool()`；
- Zod 输入/输出 Schema；
- Agent 注册和 `Mastra` Runtime；
- `generate()` 和 `maxSteps`；
- 强制 `toolChoice`；
- `tool_choice: auto`；
- 多工具自主选择；
- 多轮 Tool Loop；
- 不支持的问题不调用工具；
- 工具调用、结果、步骤和 Token usage Trace。

**推荐阅读和运行**

1. 先读 [`Mastra-学习笔记.md`](mastra-playground/Mastra-学习笔记.md) 的 Lesson 01-02；
2. 看 `src/mastra/tools/` 理解 Tool 定义；
3. 看 `src/mastra/agents/` 理解 Agent 注册；
4. 运行 `pnpm run lesson:01` 和 `pnpm run lesson:02`；
5. 再运行 `pnpm run check`、`pnpm run build`。

**后续适合研究**

Lesson 03 Workflow、Memory、RAG、Streaming/Studio，再进行 Mastra、LangGraph 和手写实现的选型复盘。

---

### 3. `mastra-short-drama-agent/`：短剧制作 Agent 主项目

**是什么**

当前唯一活跃的业务主线，目标是把短剧剧本转化为可生产的结构化资产和图像/视频提示词：

```text
剧本 → StoryBible → Scene → Shot → Prompt → Review → Refine → Export
```

**主要技术组成**

- Mastra Agent；
- Mastra Workflow；
- Zod 结构化输出；
- PostgreSQL + Prisma；
- Memory；
- 异步任务和 API；
- 资产版本、审查、反馈和导出。

**主要 Agent**

- Script Analyst；
- Scene Planner；
- Storyboard Director；
- Continuity Reviewer；
- Prompt Refiner；
- Short Drama Chat Agent。

**主要 Workflow**

- `storyUnderstandingWorkflow`；
- `scenePlanningWorkflow`；
- `storyboardProductionWorkflow`；
- `storyboardWorkflow`：历史 Spike，保留参考。

**当前真实定位**

现在的代码仍然包含此前的技术 Spike，不能把现有测试通过等同于完整产品落地。下一阶段重点是：

- 评审并落实 `docs/architecture.md`；
- 对话界面和活动流；
- 自动连续生产 Workflow；
- 资产编辑、影响分析和局部重生成；
- 问题中心、失败重试和版本管理；
- 完整导出；
- API + Worker 的可恢复部署；
- 真实 Provider、真实短剧样本和质量评估。

**接续规则**

除非用户明确说“方案确认”，否则先继续评审产品、流程和架构文档，不直接重构代码。下一次进入本目录时先读 `CONTEXT.md`。

---

### 4. `agent-base/`：Agent 底座历史基线

**是什么**

早期的 NestJS 分镜提示词优化项目，围绕任务中心、BullMQ、Prisma/PostgreSQL、LLM、盲测和镜头级生成审查循环建立了一个工程基线。

**主要内容**

- NestJS + Prisma/PostgreSQL；
- BullMQ/Redis 异步任务；
- Director → Reviewer → Refiner 镜头循环；
- 盲测侧序、逐镜评分和结果归因；
- Task API、旧接口适配和 Mock/Real 双模式；
- 架构、需求、时序、ER 图和团队评审文档。

**当前状态**

该目录已经冻结为历史参考，不再与 `mastra-short-drama-agent/` 并行扩展。它主要用于复用过去的业务决策、任务模型、盲测设计和工程经验。

**后续适合研究**

- 对比手写 Loop、NestJS 服务编排和 Mastra Workflow；
- 研究异步任务、幂等、队列、盲测和生产级 Agent 底座；
- 不要直接把旧架构当成当前短剧产品架构。

---

### 5. `agent-tool-calls/`：Tool 调用失败专题

**是什么**

针对 Agent Tool 调用失败建立的系统研究和可视化资料，不是一个运行中的业务服务。

**覆盖范围**

```text
模型生成 Tool Call
→ 协议解析
→ Input Schema
→ 参数规范化
→ 权限/状态检查
→ Tool 执行
→ Output 校验
→ 重试/降级/人工介入
→ 审计
```

重点包括：

- 未知工具和非法 JSON；
- Schema 校验失败；
- 业务规则、权限和状态失败；
- 数据库、HTTP、文件系统和 MCP 故障；
- 输出结果校验；
- 传输重试、参数重试、Tool 重试和 Run 重试；
- 幂等性、错误脱敏、预算、熔断和审计。

**文件说明**

- `agent-tools-failure-lifecycle.md`：主文章；
- `agent-tools-failure-lifecycle.html`：可交互生命周期图；
- `agent-tool-call-cases.*`：参数不一致等案例图、时序数据和视觉校验产物；
- `*.visual-check.*`：图示视觉检查生成物。

**后续适合研究**

将文档中的责任边界落到 `mastra-short-drama-agent/`：尤其是 Tool/Service 错误协议、重试预算、幂等键、取消和审计事件。

---

### 6. `agent-activity-stream/`：Agent 活动流专题

**是什么**

研究普通业务 Agent 如何做出类似 Codex 的可观察、可中断、可恢复的流式交互。重点不是暴露模型原始思维链，而是展示安全的执行活动。

**核心区分**

```text
模型原始 reasoning：默认隐藏
Agent 执行轨迹：Runtime 内部事实
公开活动流：给用户展示的安全摘要
正式回答：独立的正文流
```

**研究内容**

- `accepted`、`commentary`、`progress`、`tool_activity`、`chunk`、`sources`、终态事件；
- 活动实例的 `activityId`、`callId`、`attempt`；
- 首包和首个有意义反馈；
- 取消、断线、重连和 `Last-Event-ID`；
- 工具失败后的局部重试和 Run 重试；
- 活动完成后的折叠和摘要；
- Mastra `fullStream` 到业务公共事件的转换。

**当前状态**

协议和架构研究已经完成，但完整的 Activity Stream 表现层、首包优化、取消、重连、重试仍需要在真实浏览器和服务端环境中联合验收。

**后续适合研究**

优先结合 `mastra-short-drama-agent/` 的聊天入口，先做最小的 Run 状态和 SSE 事件协议，不要一开始构建复杂日志平台。

---

### 7. `coding-agent-ttft/`：Coding Agent 首响应延迟调研

**是什么**

一个面向 Coding Agent 的研究框架，研究从用户提交任务到看到首个有效响应的端到端延迟。

**研究维度**

- TTFT、首 chunk、首个可见内容和首个有意义内容的定义；
- Prompt、历史、代码上下文和 Tool Schema；
- 工具串行、并行、提前执行和后台异步执行；
- 模型路由、Fallback 和 Prompt Cache；
- Prefix/KV Cache、Prefill、批处理和推理服务；
- SSE、WebSocket、flush 和前端渲染；
- P50/P95/P99、质量、成本、失败率和稳定性权衡。

**文件说明**

- `outline.yaml`：研究主题、问题和并行研究分工框架；
- `fields.yaml`：研究记录字段、证据类型和 Benchmark 数据结构。

**当前状态**

目前主要是研究设计和字段定义，尚未形成完整的资料、实验结果和 Benchmark 报告。

**后续适合研究**

先完成测量口径和一个最小 Benchmark，再讨论缓存、模型路由等优化。必须同时记录质量和成本，避免只追求更低延迟。

---

### 8. `embodied-data-loop/`：具身智能数据闭环

**是什么**

面向具身智能/机器人方向的转型学习仓，核心是理解从传感器、数据采集、数据集、训练到机器人执行的完整闭环。

**主要内容**

- 具身智能全景；
- 机器人软件栈和 ROS；
- 数据采集、遥操作和数据集；
- 传感器到执行的链路；
- 平台工具和岗位 JD 映射；
- Feynman 笔记、架构图和调研产出。

**目录内重点**

- `exec/`：按天拆解的学习任务和资料；
- `outputs/`：已经完成的笔记、图、映射表和 JD 收集；
- `转型路线图.md`：长期定位；
- `转型4周计划.md`：执行计划；
- `学习进度.md`：当前接续状态。

**当前状态**

该线路当前暂停。恢复时从 `学习进度.md` 和 `exec/00-4周天级执行清单.md` 开始，不与 Agent 主线混用进度。

---

### 9. `ai-platform-interview-handbook/`：AI Platform 面试手册

**是什么**

围绕真实项目整理的 AI Platform / Tech Lead 技术面试推演文档，不是 Agent 代码项目。

**主要内容**

- 个人介绍和项目经历；
- 工程化和 Task Platform；
- AI Platform；
- Node.js、Redis、PostgreSQL；
- 系统设计；
- 团队管理和行为面试；
- Docker/Kubernetes；
- Go 基础。

**当前状态**

13 个章节已经完成，共约 196 道问题，同时保留 Markdown 源文档和 PDF 产物。

**后续用途**

作为 Agent 项目的工程知识参考，尤其适合复用：

- Redis/BullMQ；
- PostgreSQL 数据建模；
- Docker/Kubernetes 部署；
- Task Platform；
- AI Platform 系统设计。

不建议把这里的面试文档当作当前业务实现入口。

## 三、推荐学习路径

### 如果目标是补 Agent 基础

```text
Agent_Learning_Context_Lesson01_02.md
→ week-research-agent/day1/
→ week-research-agent/day2/
→ week-research-agent/day3/
→ mastra-playground/
```

### 如果目标是研究 Tool

```text
week-research-agent/day2/
→ mastra-playground Lesson 01-02
→ agent-tool-calls/
→ week-research-agent/mcp/
```

### 如果目标是继续短剧 Agent 主线

```text
mastra-short-drama-agent/CONTEXT.md
→ docs/prd.md
→ docs/product-form.md
→ docs/architecture.md
→ docs/workflow-design.md
→ docs/traceability-matrix.md
→ 再开始代码实现
```

### 如果目标是研究 Agent 产品体验

```text
agent-activity-stream/
→ mastra-short-drama-agent/ 聊天入口
→ SSE / Run 状态 / Activity Stream 联合验收
```

### 如果目标是研究 Agent 性能

```text
coding-agent-ttft/outline.yaml
→ coding-agent-ttft/fields.yaml
→ 建立最小可复现实验
→ 记录 TTFT、质量、成本和失败率
```

## 四、几个容易混淆的关系

| 容易混淆 | 实际关系 |
| --- | --- |
| `agent-base/` 与 `mastra-short-drama-agent/` | 前者是冻结的 NestJS 历史底座，后者是当前短剧主项目 |
| `week-research-agent/` 与 `mastra-playground/` | 前者手写 Python 原理实现，后者学习 Mastra 封装后的运行时 |
| Agent 与 Workflow | Agent 负责模型判断和结构化生成，Workflow 负责确定的业务步骤和状态推进 |
| Tool 与 Repository/Service | Tool 是模型可调用能力边界，Repository/Service 是确定性业务读写；当前短剧项目主要使用后者，独立 Tool 层仍可继续抽取 |
| reasoning 与 Activity Stream | reasoning 默认隐藏；Activity Stream 只展示安全的执行摘要 |
| 研究文档与生产代码 | `agent-tool-calls/`、`agent-activity-stream/`、`coding-agent-ttft/` 主要是研究资料，不代表对应能力已经完整落地 |
