{
  "name": "short-drama-monorepo",
  "private": true,
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "check": "pnpm -r check",
    "test": "pnpm --filter server test",
    "dev:server": "pnpm --filter server dev",
    "dev:web": "pnpm --filter web dev",
    "db:migrate": "pnpm --filter server db:migrate",
    "db:deploy": "pnpm --filter server db:deploy"
  }
}