# Agent 底座线路：学习进度（接续用）

> 线路目标：短剧场景 agent 能力（当前 workload：分镜提示词 agent），未来成长为短剧通用能力平台。
> 接续约定：见根目录 [`../README.md`](../README.md)。
> 最后更新：2026-08-16（周日：盲测契约 v1.1 修复后冻结 + Prisma 落地修复）

## 当前状态

**▶ 周日加固完成：盲测契约 v1.1 冻结（侧序落库/平局/防重复/服务端归因），单测 13/13，e2e 14/14；Prisma 双坑修复（client 缺包 + 7.x breaking→锁 6.x），迁移 SQL 已生成待 DB 真验证；已推送**

- 代码：`storyboard-agent/`（NestJS+Prisma+BullMQ；存储/队列/LLM/任务中心/旧接口全部双模式，mock 先行 real 桩已留）
- 验收记录：POST tasks→done→5镜 finalPrompt+rationale+reviewLog→pairs 盲测对→scores 归因→报表 ✓
- 周一架子死线已提前达成，周一剩余：接真实端点/旧接口/任务中心（填 env+验证）、DTO 冻结、发团队

## 周一行动清单（顺序即优先级）

1. 拿 LLM 端点+key → 填 env 接 real，跑真实剧本
2. DB 模式真验证：`docker compose up -d` + `npm run db:deploy`，**看日志确认无「降级内存」**（ADR-5 教训：周末 6/6 验收实际全是 mem 模式）；QUEUE_MODE=bullmq 同步验证
3. 拿旧接口文档 → 实现 callRealLegacy + zod 契约验证
4. 任务中心接口文档 → 替换 mock；确认部署环境（队友 C）
5. 知会老板：镜头级盲测；发 team-review.md 收反馈（周日晚截止）

## 周日完成（2026-08-16，盲测契约冻结前修复）

原计划「周一冻 DTO」，通读后发现四处硬伤直接修：① 侧序存内存 Map（重启丢失→盲测公平性破防）→ 落库 BlindTestOrder 表 ② winner 无平局（逼客户二选一污染数据）→ tie + tieRate ③ 不防重复提交 → 唯一约束+409 ④ POST /scores 要求客户端回传 sideOrder 但 pairs 从未返回过（客户端填不了且可污染归因）→ 服务端自查。附带发现并修复：`@prisma/client` 不在 dependencies（DB 模式静默降级没人发现）、Prisma 7 breaking（锁 6.x，ADR-5）、schema 关系字段缺失、迁移目录不存在（离线生成）。新增 test/blind-score.spec.ts（9 条）+ scripts/e2e-blind.mjs（14 断言）。

## v1 主链路（最终版）

```
POST /tasks(剧本+配置) → 任务中心建档 → BullMQ 入队
→ LegacyImporter 调旧接口拿分镜列表+旧提示词（落 OldShot=盲测基线）
→ 每镜 Shot Loop（Director→Reviewer→Refiner，通过即停≤3轮熔断）
→ 落库 + 进度回写任务中心
→ 盲测打分页：GET pairs（随机侧序随任务缓存）→ 逐镜选优+双分 → POST scores（归因落 Score 表）
```

## 已锁定决策（grilling 三会话累计）

**范围与靶心**：周五客户内测盲测打分、对比赢=成功；镜头级打分（不做整集分，专注单镜优化）；用户只定架子，4 人按模块分工；周五必须真跑（mock 不能上 demo）。

**架构**：沿用旧系统切分（旧接口导入分镜+旧提示词，Parser 降为 demo 后）；异步任务式（BullMQ worker，一集一 job，镜级并发 P=4 可配）；任务数据/状态由公司任务中心维护（适配层+mock 降级）；每镜独立 generator-critic loop（≤3 轮熔断可配，通过即停）；防穿帮=输入侧上下文注入+Reviewer 规则双管。

**技术栈**：NestJS + Prisma/PostgreSQL + BullMQ/Redis + zod + OpenAI 兼容 LLM 封装（复用原团队端点）。**不用 LangGraph**（loop 是直线循环，手写 20 行；框架负资产——决策论证已沉淀在 team-review.md FAQ）。

**盲测设计**：服务端随机侧序+随任务缓存（防顺序偏好、防记位置）；打分=逐镜选优+1-5 双分；sideOrder 归因落库可审计；砍实时看板（后台查库/导出报表）；内测小规模、token 成本不监控。

**知识源**：原团队 prompt 起点 + LLM 生成规则草稿 + 人工筛选。

## 文档包（docs/，5 件，2026-08-15 审查一致）

| 文件 | 内容 |
|---|---|
| team-review.md | 团队 review 入口：决策表/主链路/分工阅读索引/FAQ/逐人反馈清单（周日晚截止） |
| architecture.md | 完整设计：推理/数据模型（Shot/OldShot/ReviewLog/Score/CharacterCard）/API/loop/架子清单/分工/风险 |
| requirements.md | 需求：F1-F10/非功能/边界/假设/里程碑/待办 |
| storyboard-arch.svg / storyboard-sequence.svg / storyboard-er.svg | 架构/时序/ER 三图（几何验证过） |

## 时间线（硬）

- 周一下班：架子链路跑通（mock LLM 可）+ DTO 冻结
- 周二：4 人进场（A 盲测页/B prompt 调优/C 模板部署/D 测试报表）
- 周四五：联调+真实剧本+预演
- 周五下班：内测版发布（客户可达环境）

## demo 后演进方向（不预建）

- Parser 恢复自切分 → 摆脱旧切分依赖
- agent 自主化：动态轮数（Reviewer confidence）、LLM 自选动作、挂工具
- 任务中心接口替换 mock；长期记忆；模板 CRUD；LangGraph 对照实验（Lesson 08）
